import { View, Text } from 'react-native';
import React from 'react';

type Props = {
  id: string;
};

const RestaurantDetails = ({ id }: Props) => {
  return (
    <View>
      <Text>RestaurantDetails</Text>
    </View>
  );
};

export default RestaurantDetails;
